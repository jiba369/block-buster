apiVersion: apps/v1
kind : Deployment 
metadata :
  name: pract28flux
  namespace: wave2
spec:
  replicas: 10
  selector:
    matchLabels:
      pract28: "true"
  template:
    metadata:
      labels:
        pract28: "true"
    spec:
      containers:
        - name: deploypodspract28flux
          image: busybox
          command: ["/bin/sh", "-c"]
          args:
            - >
              sleep 40;
              touch /tmp/live;
              while true; do sleep 100; done;          

            
